<%= button_to "Delete Album", album_url, :method => :delete%>

<h2><%= @album.album_name %></h2>
<h3><%= link_to @album.band.band_name, band_url(@album.band) %></h3><p>

<table>
<% @album.tracks.each do |track| %>
<tr><td><%= link_to track.track_name, track_url(track) %></td></tr>
<% end %>
</table>